name: schedule
on:
    schedule:
        - cron: '*/1 * * * *'

jobs:
    test_schedule:
        runs_on: ubuntu-latest
    steps:
        - name: Echo current time
        run: echo "The current server time is $(date)"
